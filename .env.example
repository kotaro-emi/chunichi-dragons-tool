# Supabaseプロジェクトの設定値をコピーしてローカルの .env に貼り付けてください。
# .env はコミットされません（.gitignore参照）。
# ここに入るanonキーは公開前提の値で秘密情報ではありませんが（RLSが防御線）、
# 開発用と本番用で別プロジェクトを使う場合の切り替えをしやすくするため環境変数にしています。

PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
