# 中日ドラゴンズなんでもツール 開発計画書

最終更新：2026-07-27

## 0. この文書について

このドキュメントは、「中日ドラゴンズの歴史・成績・選手情報・応援歌・補強ポイント・試合速報などが一目でわかるアプリ」を、アプリ開発が初めての方でも迷わず作れるように整理した計画書です。

専門用語には都度カッコで簡単な説明を入れています。わからない言葉が出てきたら、末尾の「8. 用語集」も参照してください。

この計画書はあくまで「今の時点で決めた方針」です。実装を進める中で「思っていたより難しい／簡単だった」ということが必ず出てきます。そのときはこの文書ごと更新して構いません（`CLAUDE.md`にも「構成方針は決まり次第追記していく」と書かれています）。

---

## 1. このアプリで実現したいこと

### 1.1 機能一覧

| カテゴリ | 内容 |
|---|---|
| 球団の歴史 | 創設(1936年)から現在までの年表 |
| チーム成績 | 野手の打撃成績・守備成績、投手の投手成績 |
| 個人成績 | 選手ごとの詳細な数字 |
| 選手の経歴 | プロ入り年度、出身、経歴など |
| 応援歌 | 選手別の応援歌（歌詞の一部＋掛け声） |
| 選手の特徴 | プレースタイルなどの紹介文 |
| 補強ポイント | AIによる「今後どこを強化すべきか」の定期分析 |
| 試合結果・速報 | 試合の途中経過〜結果 |
| 訪問者アカウント | お気に入り選手登録、選手・試合へのコメント投稿 |

### 1.2 前提条件

- **利用者**：本人（自分用）がメイン。ただしサイトは今後も一般公開を続け、他のファンが見に来る可能性も考えたデザイン・運用にする
- **ログイン**：メールアドレス＋パスワード方式（詳細は3.5節）
- **UI方針**：情報量が多い画面（特に選手個別ページ）は、情報を縦に長く並べるのではなく、クリックで切り替える「**タブUI**」を基本とする
- **試合速報のリアルタイム性**：「試合中の途中経過もできるだけ近いタイミングで見たい」という要望はあるが、技術的な制約上「秒単位の完全リアルタイム」ではなく「数分〜十数分ごとの更新」が現実的な目標になる（理由は3.6節で詳しく説明）

---

## 2. 技術構成の提案

現状（`中日打者成績.html`など）は、フレームワークもビルドも使わない「自己完結HTML」が3枚あるだけです。今回、機能を大きく増やすにあたり、土台となる技術を選び直します。ここでは「一番シンプルな道」と「一番将来性がある道」を両方示した上で、推奨案を1つ提示します。

### 2.1 サイトの土台：Astro（推奨）or Next.js

|  | Astro（推奨） | Next.js |
|---|---|---|
| 学習コスト | 低い。出力がほぼ素のHTML/CSS/JSに近く、Reactのような複雑な状態管理を覚えなくても書ける | やや高い。Reactの考え方（コンポーネント・state等）を覚える必要がある |
| ルーティング | フォルダ＝ページ（ファイルベースルーティング）で直感的 | 同様にファイルベースルーティングで直感的 |
| 将来の伸びしろ | 十分あるが、ゴリゴリに動的なUIを作り込みたい場合はReact系の方が情報が多い | Reactエコシステムが巨大で、将来的な拡張の選択肢が最も多い |
| このプロジェクトとの相性 | 「今の3枚のHTMLを共通デザインの部品に分割したい」という目的に対して、最小の学習コストで届く | `CLAUDE.md`に元々書かれていた方針。Reactを本格的に学ぶこと自体が目的なら合う |

**結論**：今回は**Astro**を既定の推奨とします。理由は「一番易しいから」であり、「一番将来性があるから」ではありません。もし今後「Reactをちゃんと学びたい」「検索・フィルタなど凝ったUIをどんどん作り込みたい」という気持ちが強ければ、Next.jsに変更しても大きな失敗にはなりません（考え方の多くは共通しています）。

### 2.2 その他の技術要素

- **静的サイト書き出し（Static Export）＋GitHub Pages**：AstroもNext.jsも、最終的に「ただのHTML/CSS/JSファイル一式」を書き出す設定（静的サイト書き出し）ができます。これを使えば、これまで通りサーバーやデータベースの運用を増やさずに、GitHub Pagesにそのまま置くだけで公開できます
- **データはコード内のJSON/TSファイル**：既存の`PLAYERS`配列と同じ考え方の延長です。編集頻度が低いデータ（成績・応援歌・歴史・補強ポイント）は、データベースではなく普通のJSONファイルとして`data/`フォルダに置きます。ルーティン（後述）は「JSONファイルを書き換えてpushする」だけで完結し、データベース接続やAPIキー管理といった初心者には難しい概念を増やさずに済みます
- **Tailwind CSS**：クラス名を組み合わせるだけでモダンなデザインが作れるCSSの仕組みです。既存ページで使っているCSS変数（ライト/ダーク切替の仕組み）の考え方はそのまま引き継げます
- **GitHub Actions（自動ビルド・自動公開の仕組み）**：AstroもNext.jsも「ビルド」という変換作業が必要になります。これは現状のHTML直置きにはなかった工程です。GitHub Actionsという仕組みを新しく導入し、「pushされたら自動でビルド→公開まで行う」役割を持たせます。ルーティン側は今まで通り「ファイルを書き換えてpushする」だけでよく、ビルド・公開の面倒はActionsに任せます

### 2.3 比較検討：素のHTML/CSS/JSのまま拡張する案

「フレームワークを覚えるのが大変そうなら、今のまま素のHTML/CSS/JSを増やしていく」という選択肢もあります。ページ数が少ないうちは十分成立しますが、今回のように画面が7〜8個に増え、共通のヘッダーやカードデザインを使い回したい場合、同じコードのコピー＆ペーストが増えて管理が大変になっていきます。長期的な管理のしやすさを優先し、フレームワーク移行を推奨します。

---

## 2.5 新しいリスク：ビルドが初めて「壊れうる箇所」になる

これは今回の技術移行で最も見落としやすい注意点なので、独立した節として明記します。

- **今の状態**：ルーティンがHTMLファイルを直接書き換えてpushしているだけなので、万一データがおかしくなっても、**サイトは必ず表示されます**（表示内容がおかしいだけで、真っ白にはならない）
- **移行後に変わること**：GitHub Pagesでの公開が「GitHub Actionsのビルド成功」に依存するようになります。もしルーティンが壊れたJSON（文法ミスなど）をpushすると、ビルドが失敗し、**サイトが更新されなくなる（古いままになる）のに気づきにくい**、という新しいリスクが生まれます

### 対策

1. ルーティンがcommitする前に、JSONの形式が正しいかチェック（バリデーション）する一手間を入れる
2. ビルドが失敗したときに気づける仕組み（GitHub Actionsの失敗通知メールなど）を用意する
3. 開発の一番最初（Phase 0）で、**わざと壊れたデータをpushしてビルドを失敗させてみる**練習をしておき、「壊れ方」と「気づき方」を先に体験しておく

---

## 3. 画面構成（情報アーキテクチャ）

| パス | 内容 |
|---|---|
| `/` | トップページ。各コンテンツへの導線をまとめたダッシュボード |
| `/players` | 選手一覧。「打者」「投手」をタブで切り替える構成にし、指標がまったく違う打撃成績と投手成績を1つの表に無理やり混ぜない |
| `/players/[選手名]` | 選手個別ページ（後述3.1参照）。このアプリの「一目でわかる」要件の中心 |
| `/history` | 球団の歴史。年表形式 |
| `/reinforcement` | 補強ポイント。AIによる定期分析＋「公式見解ではない」旨の注記 |
| `/scores` | 試合結果・速報（3.6節の仕組みで更新） |
| `/login` | ログイン導線（サインアップ／ログイン／パスワードを忘れた場合の3画面） |
| `/mypage` | ログインした訪問者専用の「お気に入り選手」一覧 |
| `/privacy` | プライバシー・利用規約の簡易ページ |

### 3.1 選手個別ページの構成（タブUI）

情報を縦に全部並べるのではなく、次の4タブで切り替えます。

1. **プロフィール・経歴**：出身、プロ入り年度、経歴など
2. **成績**：選手の役割（`role`）によって中身を出し分ける
   - 野手（打者）：**打撃成績**（打率・本塁打・打点など）＋**守備成績**（守備位置・刺殺・補殺・失策・守備率など）
   - 投手：**投手成績**（防御率・勝敗・セーブ・奪三振・投球回など）
   - ※野手に投手成績の項目を出したり、その逆をしたりはしない
3. **応援歌**：歌詞の一部＋掛け声（著作権への配慮は4.2節参照）
4. **特徴**：プレースタイルなどの紹介文

ログインしている場合は、ページ内に「お気に入り登録ボタン」と「コメント欄」も表示します。

---

## 3.5 訪問者アカウント機能（お気に入り登録・コメント投稿）

これまでの「完全に静的・サーバーもデータベースもなし」という前提を部分的に見直す、今回最もリスクが高い追加要素です。慎重に設計します。

### 3.5.1 技術選定：Supabase

**Supabase**（スーパベース）という「BaaS」（**バックエンド・アズ・ア・サービス**＝自分でサーバーを構築・運用しなくても済むクラウドサービス）を使います。認証（ログイン機能）とデータベース（お気に入り・コメントの保存）が1つにまとまっており、無料枠があり、初心者向けの解説記事も多いのが理由です。

似た選択肢にFirebaseがありますが、お気に入り・コメントのような「表形式のデータ」を扱うなら、一般的なデータベースの仕組みであるSQL（Postgres）を採用しているSupabaseの方が相性が良いと判断しました。

コメント機能だけを最小構成にしたいなら、GitHubのコメント欄を流用する「giscus」のような外部ウィジェットもありますが、お気に入り機能には使えないため、両方欲しい場合は結局Supabase一本化がシンプルです。

### 3.5.2 データの2系統を混同しない

- `data/*.json`（ビルドを伴う）＝ルーティンが定期的に書き換える「編集コンテンツ」（成績・応援歌・歴史・補強ポイント分析）
- Supabase（ビルドを伴わない）＝訪問者が自分のブラウザからリアルタイムに書き込む「ユーザー生成データ」（お気に入り・コメント・試合速報）

この2つは目的も更新方法もまったく別物です。「なぜ試合速報だけJSONじゃないのか」と迷ったら、この違いを思い出してください。

### 3.5.3 ログイン方式：メールアドレス＋パスワード

ソーシャルログイン（Googleでログイン、など）ではなく、**メールアドレス＋パスワード方式**を採用します。

Supabase Authはメール＋パスワード認証を標準機能として持っており、以下は自作する必要がありません。

- パスワードの安全な保存（ハッシュ化）
- パスワード再設定メールの送信
- メールアドレス確認（確認メールをクリックするまで本登録扱いにしない）

ただし、ソーシャルログインの「ボタン1つ」で済む方式に比べると、作る画面は増えます（サインアップ／ログイン／パスワードを忘れた場合、の3画面）。

**最低限のガードレール**（Supabase側の初期設定を有効にするだけでOK）：
- メール確認を有効化する（確認前のアカウントではログインできないようにする）
- パスワードの最小文字数など基本的な設定を使う

### 3.5.4 【必須】セキュリティ設定：RLS

> **これは後回しにできない、最優先の必須設定です。**

静的サイトでは、Supabaseへの接続キーが公開されるJavaScriptファイルに含まれます。つまり、**RLS（Row Level Security＝行単位のアクセス制御）が唯一の防御線**になります。RLSを有効にし忘れると、誰でも他人のデータを読み書き・削除できてしまいます。

設定するルール：
- 全テーブルでRLSを必ず有効化する
- `favorites`テーブル：本人のデータのみ読み書き・削除できる
- `comments`テーブル：投稿はログインユーザーのみ、閲覧は誰でも可、編集・削除は投稿本人のみ

### 3.5.5 【必須】コメント運用のルール

実在の選手について投稿されるコメントである以上、以下は「後で追加する改善」ではなく、コメント機能を公開する時点での最低条件とします。

- ログイン必須（匿名投稿は不可）
- コメントの文字数上限を、見た目上の制限だけでなくデータベース側の制約としても設定する（見た目の制限は簡単に回避されるため）
- 管理者（運営者本人）が不適切なコメントをいつでも削除できる手段を用意する（最初は専用の管理画面は不要。Supabaseの管理画面から直接削除できれば十分）

### 3.5.6 プライバシー・利用規約への軽い言及

訪問者のメールアドレスを扱い、投稿されたコメントを公開する以上、`/privacy`ページに簡単に以下を書きます（本格的な法律文書までは不要）。

- 収集する情報（メールアドレスなど）
- コメントは公開され、運営判断で削除されうること
- 削除依頼の連絡先

---

## 3.6 試合結果・速報の実現方式

「試合中の途中経過もできるだけ近いタイミングで見たい」という要望を、2章で決めた「静的サイト書き出し＋GitHub Actionsビルド」の仕組みにそのまま乗せると、問題が起きます。

### 問題点

Astro/Next.jsの構成では、データが変わるたびにGitHub Actionsが「ビルド→デプロイ」をやり直す必要があります。試合中に数分おきでコミット・ビルドを繰り返すと：

1. ビルドのたびに数十秒〜数分の遅延が乗るため、「近いタイミング」からどんどん遠ざかる
2. gitの履歴が試合中のスコア更新コミットで埋め尽くされる
3. GitHub Actionsの無料枠（実行時間）を早く消費してしまう

### 解決の方向性

試合速報のような「頻繁に変わるデータ」は、3.5節で導入したSupabaseのデータベースに直接書き込みます。訪問者のブラウザは、サイトの再ビルドを待たずに、直接そのデータを読みに行きます（`/scores`ページを開くと一定間隔で自動的に最新値を取得し直す＝**ポーリング**という仕組みを使う）。

つまり、3.5.2節で整理した「データの2系統」に、試合速報も仲間入りします。

### データ取得の現実解

NPB公式サイトの試合速報ページ（スコアボード）を情報源として想定していますが、具体的なページ構造や、頻繁なアクセスによるサーバー負荷を避けるための適切な間隔設定（`CLAUDE.md`の「各サイトの利用規約に従う」方針の踏襲）は、実装フェーズで改めて確認します。

### 正直な期待値

claude.aiルーティンでの定期実行にも最小間隔の制約があり得るため、「秒単位の完全リアルタイム」ではなく「**数分〜十数分ごとの更新**」を現実的な目標とします。期待値のズレを防ぐため、サイト上にも「更新は数分おきです」といった注記を出すことを検討します。

---

## 4. データ構造の設計方針

### 4.1 主なデータファイル

- **`data/players.json`**：選手ごとに「経歴・特徴・応援歌情報・成績」を1つのオブジェクトにまとめます（既存の`PLAYERS`配列に経歴・特徴・応援歌フィールドを追加する形で移行）。選手には`role`（`batter`＝野手／`pitcher`＝投手）を持たせ、成績は役割に応じて出し分けます。

  ```jsonc
  {
    "name": "阿部寿樹",
    "role": "batter",
    "profile": { "born": "...", "draftYear": 2013, "bio": "..." },
    "features": "勝負強い右の中軸打者",
    "cheerSong": { "lyricsExcerpt": "...", "callLine": "かっとばせー！ あべ！" },
    "battingStats": { "avg": .278, "hr": 3, "rbi": 19, "...": "..." },
    "fieldingStats": { "position": "三塁手", "putouts": 0, "assists": 0, "errors": 0, "...": "..." }
  }
  ```

  投手の場合は`battingStats`/`fieldingStats`の代わりに`pitchingStats`（防御率・勝敗・セーブ・奪三振・投球回など）を持ちます。

- **`data/history.json`**：時代（年表）ごとのセクションに構造化
- **`data/reinforcement.json`**：生成日付つきのAI分析エントリの配列（履歴を残せる形）

### 4.2 既存データの移行元と注意点

| 移行先 | 移行元 |
|---|---|
| `players.json`の打撃成績 | `中日打者成績.html`のPLAYERS配列 |
| `players.json`の応援歌 | `中日ドラゴンズ選手別応援歌辞典.html`のカードHTML |
| `history.json` | `中日ドラゴンズの歴史.txt` |

**著作権への配慮**：既存の応援歌HTMLは「著作権の関係上、全文の掲載は控えます」という方針で歌詞を一部省略しています。`players.json`へ移行する際も、この省略方針をそのまま引き継ぎ、全文掲載などの後退が起きないようにします。

### 4.3 投手成績・守備成績の追加

既存`中日打者成績.html`は、直近のコミットで意図的に投手を除外し、守備成績も元々扱っていませんでした。今回のアプリでは、ユーザー要望によりどちらも追加します。

データソースは、NPB公式サイトの投手成績ページ・守備成績ページ（打撃成績ページ`idb1_d.html`に対応する投手版・守備版のページ）を想定していますが、具体的なURLや列構成は実装フェーズで確認します。既存の打撃成績取得ルーティンと並行して、投手成績・守備成績の取得ルーティンを新設します。

---

## 5. 自動化の仕組み

「できるだけ全部自動化したい」という要望への回答として、何が自動化しやすく、何がしにくいかを正直に整理します。

| データ | 自動化のしやすさ | 方針 |
|---|---|---|
| 打撃成績・守備成績・投手成績 | しやすい | 打撃は既に稼働中。守備・投手成績も同じ仕組みで追加できる見込み |
| 試合結果・速報 | 高頻度な自動化が必要 | Supabase経由でビルドを伴わずに更新（3.6節） |
| 補強ポイント分析 | 自動生成は可能だが「解釈」を含む | AI定期生成＋公式見解ではない旨の注記が必須 |
| 経歴・応援歌 | 情報源が散在し変化頻度も低い | 「完全自動化」より「半年に一度など低頻度の自動チェック＋人手確認」が現実的 |

### 5.1 成績データの更新頻度：「毎試合ごと」

「毎試合ごとに更新したい」という要望を踏まえ、打撃成績・守備成績・投手成績は「試合終了後、都度更新」を目標とします。中日の試合は通常1日1試合のため、実質的には現行の「1日1回」ルーティンとほぼ同じ運用で足ります。ただし、ダブルヘッダーや連戦が続く時期も考慮し、「日付固定」ではなく「試合実施日ベース」でルーティンを設計する方針とします。

### 5.2 補強ポイント分析の安全策

AIが生成した内容を検証なしにそのまま自動公開する設計にはリスクがあります（実在の選手について不適切・的外れな分析が無審査で公開され続ける可能性）。「AIによる分析であり公式見解ではない」という注記に加え、以下のいずれかの方針を採用します。

1. 生成結果に`draft`（下書き）フラグを持たせ、本人が確認してから表示に反映する
2. 当面は無審査で自動公開する運用リスクを受け入れる（本人が選ぶ）

### 5.3 フレームワーク移行後もルーティンの仕事はシンプルなまま

フレームワーク移行後も、ルーティンが行う操作は「データJSONを書き換えてpush」のみに据え置きます。ビルド・公開はGitHub Actionsに任せることで、ルーティン側の変更を最小限にします。

---

## 6. デザイン方針（モダンなデザイン要件への対応）

- 既存3ページで統一されているCSS変数ベースのライト/ダーク対応・明朝＋ゴシックのフォント方針を継続する
- 中日ブルー基調＋差し色のカラーパレット（既存の配色があればそれを踏襲）
- カードベースのレイアウト、レスポンシブ対応（スマホ表示前提）※あくまで目指す方向性であり、コントラスト比や操作性の厳密なチェックまでは今回の計画に含めない
- **タブUI**：情報量の多い画面（選手個別ページ、選手一覧の打者/投手切り替えなど）はクリックで表示を切り替えるタブ形式を基本パターンとする。Tailwind CSSだけでも実装できる軽量なもので、追加のUIライブラリ導入は必須ではない
- Tailwind CSSでのモダンなUI例（角丸・影・余白の取り方など）を実装時に具体化する
- 現在の`index.html`（リダイレクト専用ページ）は役目を終える：フレームワーク移行後はビルド後の出力自体がトップページになるため、廃止（またはビルド設定で明示的に置き換え）する

---

## 7. 開発ロードマップ（フェーズ分け）

| フェーズ | 内容 |
|---|---|
| **Phase 0** | フレームワーク環境構築＋GitHub Actions自動公開パイプラインの疎通確認（中身は仮でよいので「動く」ことを確認）。**加えて、わざと壊れたJSONをpushしてビルドを失敗させ、どう気づけるかを事前に体験しておく**（2.5節のリスク対策の実地確認） |
| **Phase 1** | **デザインモック作成**。本物のデータを繋ぎ込む前に、ダミーデータ（選手1〜2名分など）だけを使ってトップページ・選手個別ページを試作し、配色・フォント・カードデザイン・ダークモードといった「モダンな見た目」の方向性を先に固める |
| **Phase 2** | 既存3ファイルのデータをJSON化して移行。あわせて投手成績・守備成績のデータソース確認・取得ルーティン新設も行う |
| **Phase 3** | 選手一覧・選手個別ページ・歴史ページの実装（Phase 1で固めたデザイン・タブUIを本実装に反映） |
| **Phase 4** | 補強ポイントAI定期分析の仕組み構築 |
| **Phase 5** | **Supabase基盤構築**（共通インフラ）。試合速報・訪問者アカウントの両方が土台にするため、先にまとめて1回作る：プロジェクト作成→テーブル設計→**RLSポリシー設定（後回しにしない）** |
| **Phase 6** | 試合結果・速報機能。Phase 5の基盤の上で、まずは認証不要・読み取り専用のシンプルな仕組みとして構築（NPB公式サイトのスクレイピング頻度・利用規約確認を含む） |
| **Phase 7** | 訪問者アカウント機能（お気に入り・コメント）。ログイン→お気に入り→コメント→運営者用の削除手段とプライバシーページの順で積み上げる。最もリスクが高い追加要素のため、他のコンテンツ表示が安定した後の終盤フェーズとする |
| **Phase 8** | **デザイン仕上げ**。全ページに統一デザインを行き渡らせ、レスポンシブ対応・ダークモードの最終調整・細部のアニメーションなど「おしゃれさ」を仕上げる |

将来構想として、選手インタビューやニュース・トレード情報のまとめなど、さらなる拡張も考えられますが、今回のフェーズには含めません。

---

## 8. 用語集

| 用語 | 説明 |
|---|---|
| Astro / Next.js | ページをフォルダ構成で作れるWebサイト用フレームワーク。素のHTML/CSS/JSを書きやすく整理してくれる道具 |
| React | Next.jsの土台になっている、画面の部品（コンポーネント）を組み合わせて作るJavaScriptの仕組み |
| 静的サイト書き出し | フレームワークで作ったサイトを「ただのHTML/CSS/JSファイル」に変換すること。サーバー不要でGitHub Pagesなどにそのまま置ける |
| GitHub Actions | GitHubに用意されている「pushされたら自動で何かする」仕組み。今回はビルド＋公開の自動化に使う |
| JSON | データを保存するためのシンプルなテキスト形式。今回は成績・応援歌・歴史などのデータを入れる箱として使う |
| リポジトリ | プロジェクトのファイル一式を管理する場所（GitHub上のこのプロジェクトのこと） |
| ルーティン | claude.aiのスケジュール実行クラウドエージェント。決まった時間に自動でデータ取得・書き換え・pushを行う |
| Supabase | 認証（ログイン）とデータベースをまとめて提供してくれるクラウドサービス（BaaS） |
| BaaS | バックエンド・アズ・ア・サービス。自分でサーバーを構築・運用しなくても済む、クラウド上の裏方サービス |
| 認証（ログイン） | 「その人が誰であるか」を確認する仕組み |
| RLS（Row Level Security） | データベースの「行単位のアクセス制御」。「このデータは本人しか読み書きできない」といったルールを設定する仕組み |
| ポーリング | 一定間隔で「最新のデータある？」と繰り返し取得しにいく方式。真のリアルタイムではないが、それに近い体験を作れる |
| ビルド時データ / ランタイムデータ | ビルド時データ＝サイトを作る（ビルドする）ときに一緒に焼き込まれるデータ（JSONファイルなど）。ランタイムデータ＝訪問者がページを開いた後にリアルタイムで取得・書き込みされるデータ（Supabase側） |
| タブUI | クリックで表示内容を切り替える画面部品。1画面に情報を詰め込みすぎず、見たい情報だけを表示できる |
